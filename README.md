# BXTrack Frontend Test

I've used CRA for the base React template (not Typescript), Prime React Components as a component library, and sass for styling. You can run the application with standard:
- npm start
